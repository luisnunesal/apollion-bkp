import { GridBreakpoints } from '../../containers/Grid';

export const DefaultLayoutMedias: GridBreakpoints = {
  xs: {
    columns: '100%',
    rows: '100%',
    areas: `
      "content"
    `,
  },
};
